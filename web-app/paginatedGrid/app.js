Ext.application({
    name: 'FORM',
    //application name that becomes the namespace
    // automatically create an instance of FORM.view.Viewport
    autoCreateViewport: true,
    controllers: [
        'Users']
});